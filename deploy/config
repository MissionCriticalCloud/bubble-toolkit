[mct]
# Needs trailing slash
basedir = /data/shared/
templatedir = /data/templates/
imagedir = /data/images/

# Relative to basedir
offeringdir = offering
hardwaredir = hardware
firstbootdir = firstboot
roledir = role
clouddir = cloud
