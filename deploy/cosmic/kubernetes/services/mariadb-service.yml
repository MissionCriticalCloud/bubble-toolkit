apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
spec:
  selector:
    app: mariadb
  ports:
    - port: 3306
      protocol: TCP
      targetPort: 3306
      nodePort: 30060
  type: NodePort
